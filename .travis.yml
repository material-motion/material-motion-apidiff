language: objective-c
osx_image: xcode10
sudo: false
notifications:
  email: false
script:
  - set -o pipefail
  - xcodebuild -workspace "apple/diffreport.xcworkspace" -scheme "diffreportlib" -configuration "Debug" -destination "platform=macOS" -enableCodeCoverage "YES" test
after_success:
  - bash <(curl -s https://codecov.io/bash)
